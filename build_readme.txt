This project uses a Visual Studio solution. To build on CI, the workflow assumes msbuild is available on the runner.

If you want CMake support or cross-platform builds, add a CMakeLists.txt and update the GitHub Actions workflow accordingly.
